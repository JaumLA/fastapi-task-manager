Projeto simples e direto que possui usuários e tarefas para entender melhor os conceitos do FastAPI.

Para executar e testar use: docker compose up --build

Para desenvolver use: docker compose up --build --watch